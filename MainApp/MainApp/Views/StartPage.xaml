<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:colores="clr-namespace:MainApp.Models"
             x:Class="MainApp.Views.StartPage"
             BackgroundColor="{x:Static colores:Styles.BackgroundColor}">
    <ContentPage.Content>
        <StackLayout>
            <Label x:Name="GreetingLabel"
                FontSize="30" TextColor="{x:Static colores:Styles.StartPageTextColor}"
                HorizontalTextAlignment="Center"
                VerticalOptions="CenterAndExpand"
                HorizontalOptions="CenterAndExpand" />
            <Image Source="icon.png" HorizontalOptions="CenterAndExpand"/>
            <Button x:Name="StartButton"
                    BorderRadius="0"
                    BackgroundColor="Gray"
                    TextColor="White"
                    Clicked="Button_Clicked"/>
            <Label x:Name="MadeBy"
                FontSize="10" TextColor="{x:Static colores:Styles.StartPageTextColor}"
                VerticalOptions="EndAndExpand" 
                HorizontalOptions="CenterAndExpand" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>